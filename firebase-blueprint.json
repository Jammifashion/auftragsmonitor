{
  "entities": {
    "Order": {
      "title": "Order",
      "description": "Represents a customer order, internal structure task, or callback.",
      "type": "object",
      "properties": {
        "title": { "type": "string", "description": "Short title of the entry" },
        "clientId": { "type": "string", "description": "Reference to the client document ID" },
        "clientName": { "type": "string", "description": "Redundant customer name for fast display" },
        "projectId": { "type": "string", "description": "Reference to the project document ID" },
        "projectName": { "type": "string", "description": "Redundant project name for fast display" },
        "description": { "type": "string", "description": "Detailed description or task content" },
        "deadline": { "type": "string", "format": "date-time", "description": "When it is due" },
        "type": {
          "type": "string",
          "enum": ["order", "structure", "callback"],
          "description": "The category of this entry"
        },
        "status": {
          "type": "string",
          "enum": ["pending", "in_progress", "completed", "cancelled"],
          "description": "Current status"
        },
        "priority": {
          "type": "string",
          "enum": ["low", "medium", "high"],
          "description": "Urgency level"
        },
        "structured_details": { "type": "object", "description": "Bento-style structured info" },
        "rawInput": { "type": "string", "description": "Original input text" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "userId": { "type": "string", "description": "Owner ID" }
      },
      "required": ["title", "status", "type", "userId", "createdAt"]
    },
    "Client": {
      "title": "Client",
      "description": "Customer contact information (CRM).",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "telefon": { "type": "string", "nullable": true },
        "email": { "type": "string", "nullable": true },
        "adresse": { "type": "string", "nullable": true },
        "zahlungsinfo": { "type": "string", "nullable": true },
        "updatedAt": { "type": "string", "format": "date-time" },
        "userId": { "type": "string" }
      },
      "required": ["name", "userId", "updatedAt"]
    },
    "Project": {
      "title": "Project",
      "description": "Master data for a project (e.g., app development).",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string", "nullable": true },
        "status": { "type": "string", "enum": ["active", "completed", "paused"] },
        "updatedAt": { "type": "string", "format": "date-time" },
        "userId": { "type": "string" }
      },
      "required": ["name", "userId", "updatedAt"]
    },
    "User": {
      "title": "User",
      "description": "User specific settings.",
      "type": "object",
      "properties": {
        "theme": { "type": "string" },
        "accent": { "type": "string" },
        "toneOfVoice": { "type": "string" },
        "recognitionLanguage": { "type": "string" },
        "defaultDeadline": { "type": "string" },
        "autoArchive": { "type": "boolean" }
      }
    }
  },
  "firestore": {
    "orders": {
      "schema": "Order",
      "description": "Collection of all customer orders."
    },
    "clients": {
       "schema": "Client",
       "description": "Invisible CRM collection."
    },
    "projects": {
       "schema": "Project",
       "description": "Project master data."
    },
    "users": {
       "schema": "User",
       "description": "User specific settings."
    }
  }
}
